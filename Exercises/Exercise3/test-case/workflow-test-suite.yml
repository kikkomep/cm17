##########################################################################################
#  Global settings
##########################################################################################
galaxy_url:     "http://192.168.99.100:30700"            # default is GALAXY_URL
galaxy_api_key: "0dd81a04a73556e4ff98ed7c527535db"       # default is GALAXY_API_KEY
enable_debug:   "False"                                  # enable debug level
output_folder:  "results"                                # path folder for outputs

##########################################################################################
#  Workflow tests
##########################################################################################
workflows:

  ########################################################################################
  # workflow test case: "workflow_test_case_1"
  ########################################################################################
  workflow_test_case_1:

    # worflow definition file (i.e., `.ga`file)
    file: "workflow.ga"

    # input section
    #######################################################################################
    inputs:
    
      # short form
      # zipped_data: "inputs/netcdf.zip"

      # extended form
      zipped_data:
        file: "inputs/netcdf.zip"
        type: "zip"
    
      # short form
      # tracing_model: "inputs/tracing_model.csv"

      # extended form
      tracing_model:
        file: "inputs/tracing_model.csv"
        type: "csv"
    
      # short form
      # sbml_model: "inputs/sbml_model.sbml"

      # extended form
      sbml_model:
        file: "inputs/sbml_model.sbml"
        type: "xml"
    
      # short form
      # inputExchange: "inputs/exchanged.csv"

      # extended form
      inputExchange:
        file: "inputs/exchanged.csv"
        type: "csv"
    
      # short form
      # constraints: "inputs/constraints.csv"

      # extended form
      constraints:
        file: "inputs/constraints.csv"
        type: "csv"
    
    # expected outputs
    #######################################################################################
    expected:
    
      # short form
      # constrained_sbml_model: "expected/constrained_sbml_model.xml"

      # extended form
      constrained_sbml_model:
        file: "expected/constrained_sbml_model.xml"
        comparator: "filecmp.cmp"
    
      # short form
      # best_fit_fluxes: "expected/best_fit_fluxes.csv"

      # extended form
      best_fit_fluxes:
        file: "expected/best_fit_fluxes.csv"
        comparator: "filecmp.cmp"
    
      # short form
      # best_fit_label: "expected/best_fit_label.csv"

      # extended form
      best_fit_label:
        file: "expected/best_fit_label.csv"
        comparator: "filecmp.cmp"
    
